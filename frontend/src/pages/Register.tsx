import { registerUser } from "../services/auth.service";

export default function(){

    const submit = async(e:any) =>{
        e.preventDefault();
    };
    
    return(
        <div>
            Register
        </div>
    );
}